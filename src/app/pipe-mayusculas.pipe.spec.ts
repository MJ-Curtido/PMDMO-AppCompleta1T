import { PipeMayusculasPipe } from './pipe-mayusculas.pipe';

describe('PipeMayusculasPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeMayusculasPipe();
    expect(pipe).toBeTruthy();
  });
});
